{
  "hash": "95d2eea742253ef8ef0f2f67205b7d03",
  "result": {
    "engine": "jupyter",
    "markdown": "# Matching Estimators\n\n\n::: {#821646ac .cell execution_count=2}\n``` {.julia .cell-code}\nusing DataFrames\nusing GLM\nusing Statistics\nusing Random\nusing LinearAlgebra\nusing Printf\nusing CairoMakie\n```\n:::\n\n\nMatching is the oldest and most-taught approach to observational causal\ninference. For each treated unit, find a \"twin\" control unit with\nsimilar covariates, then compare outcomes within matched pairs. With\nenough good matches, the comparison approximates a randomised\nexperiment.\n\nThe mature implementation in R (`MatchIt`, `cobalt`, `WeightIt`) has no\ndirect Julia counterpart. This chapter implements the core ideas\ninline — propensity score matching, balance diagnostics, weighted\nestimation — using `GLM.jl` and base Julia. For optimal matching, full\nmatching, and coarsened exact matching with mature tooling, see the\n[R companion chapter](https://xiangao.github.io/causal_econometrics_guide/matching.html).\n\n## Assumptions\n\nMatching identifies a causal effect under three assumptions:\n\n1. **SUTVA** — no interference, no hidden treatment versions.\n2. **Ignorability** — conditional on $X$, $D \\perp (Y(0), Y(1))$.\n3. **Overlap** — every $x$ has positive probability of both treatments.\n\n## A simulated example\n\n::: {#dgp .cell cache='true' execution_count=3}\n``` {.julia .cell-code}\nRandom.seed!(42)\nn  = 2000\nX1 = randn(n)\nX2 = randn(n)\n\n# Treatment depends on covariates (confounding)\nps_true = @. 1 / (1 + exp(-(-0.5 + 0.6 * X1 + 0.4 * X2)))\nD       = Float64.(rand(n) .< ps_true)\n\n# Outcome: true treatment effect = 1, plus confounding through X1, X2\nY = @. 1.0 * D + 0.8 * X1 + 0.6 * X2 + 0.5 * randn()\n\ndf = DataFrame(Y = Y, D = D, X1 = X1, X2 = X2)\n@printf(\"Naive difference in means: %.3f\\n\", mean(Y[D .== 1]) - mean(Y[D .== 0]))\n@printf(\"True ATE: 1.000\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nNaive difference in means: 1.713\nTrue ATE: 1.000\n```\n:::\n:::\n\n\n## Estimating the propensity score\n\n::: {#ps .cell cache='true' execution_count=4}\n``` {.julia .cell-code}\nps_fit = glm(@formula(D ~ X1 + X2), df, Binomial(), LogitLink())\ndf.ps  = predict(ps_fit)\n\n@printf(\"PS range: [%.3f, %.3f]\\n\", minimum(df.ps), maximum(df.ps))\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nPS range: [0.060, 0.927]\n```\n:::\n:::\n\n\n## Nearest-neighbour 1:1 matching\n\n::: {#nn-match .cell cache='true' execution_count=5}\n``` {.julia .cell-code}\n\"\"\"\n    nearest_neighbor_match(ps_treated, ps_control)\nReturn a vector of indices into the control group, matching each treated\nunit to its nearest-neighbour control on PS. Sampling is without replacement.\n\"\"\"\nfunction nearest_neighbor_match(ps_treated, ps_control)\n    n_t = length(ps_treated)\n    avail = trues(length(ps_control))\n    matches = zeros(Int, n_t)\n    for (i, p) in enumerate(ps_treated)\n        candidates = findall(avail)\n        if isempty(candidates)\n            break\n        end\n        diffs = abs.(ps_control[candidates] .- p)\n        best  = candidates[argmin(diffs)]\n        matches[i] = best\n        avail[best] = false\n    end\n    matches\nend\n\nidx_T = findall(df.D .== 1)\nidx_C = findall(df.D .== 0)\n\nmatched_C = nearest_neighbor_match(df.ps[idx_T], df.ps[idx_C])\nmatched_idx_C = idx_C[matched_C[matched_C .> 0]]\nmatched_idx_T = idx_T[1:length(matched_idx_C)]\n\n@printf(\"Matched %d treated to %d controls\\n\",\n        length(matched_idx_T), length(matched_idx_C))\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nMatched 818 treated to 818 controls\n```\n:::\n:::\n\n\n## Balance diagnostics\n\nThe standardised mean difference (SMD) before and after matching shows\nwhether the matched groups are balanced:\n\n::: {#balance .cell cache='true' execution_count=6}\n``` {.julia .cell-code}\nfunction smd(x_t, x_c)\n    (mean(x_t) - mean(x_c)) / sqrt((var(x_t) + var(x_c)) / 2)\nend\n\n@printf(\"%-15s %12s %12s\\n\", \"Covariate\", \"SMD before\", \"SMD after\")\nfor v in [:X1, :X2]\n    smd_before = smd(df[!, v][df.D .== 1], df[!, v][df.D .== 0])\n    smd_after  = smd(df[!, v][matched_idx_T], df[!, v][matched_idx_C])\n    @printf(\"%-15s %12.3f %12.3f\\n\", String(v), smd_before, smd_after)\nend\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nCovariate         SMD before    SMD after\nX1                     0.614        0.270\nX2                     0.394        0.152\n```\n:::\n:::\n\n\nStandardised mean differences of magnitude < 0.1 after matching indicate\nadequate balance. Larger differences suggest the matching is insufficient\nand a more flexible method (e.g. Mahalanobis distance, or matching with\nreplacement) is needed.\n\n## Estimating the ATT on matched data\n\nAfter matching, the ATT is the simple difference in means:\n\n::: {#att .cell cache='true' execution_count=7}\n``` {.julia .cell-code}\natt = mean(df.Y[matched_idx_T]) - mean(df.Y[matched_idx_C])\n@printf(\"Matched ATT: %.3f  (true ATE = 1.000)\\n\", att)\n\n# Standard error: paired-difference SE (treats matched pairs as paired)\ndiffs = df.Y[matched_idx_T] .- df.Y[matched_idx_C]\nse    = std(diffs) / sqrt(length(diffs))\n@printf(\"Paired-diff SE: %.3f\\n\", se)\n@printf(\"95%% CI: [%.3f, %.3f]\\n\", att - 1.96 * se, att + 1.96 * se)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nMatched ATT: 1.287  (true ATE = 1.000)\nPaired-diff SE: 0.032\n95% CI: [1.225, 1.350]\n```\n:::\n:::\n\n\nThe matched estimate is close to the truth, and the paired-difference\nstandard error is the right inferential approach for matched pairs.\n\n## Matching with replacement\n\nWhen the treated group is larger or the propensity distributions\ndiverge, allowing each control to be matched more than once improves\nbalance at the cost of effective sample size:\n\n::: {#nn-replace .cell cache='true' execution_count=8}\n``` {.julia .cell-code}\nfunction nn_match_replace(ps_treated, ps_control)\n    [argmin(abs.(ps_control .- p)) for p in ps_treated]\nend\n\nmatched_repl = nn_match_replace(df.ps[idx_T], df.ps[idx_C])\n@printf(\"Average reuse: %.2f (1.0 = no replacement)\\n\",\n        length(matched_repl) / length(unique(matched_repl)))\n\natt_repl = mean(df.Y[idx_T]) - mean(df.Y[idx_C[matched_repl]])\n@printf(\"Matched-with-replacement ATT: %.3f\\n\", att_repl)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nAverage reuse: 1.76 (1.0 = no replacement)\nMatched-with-replacement ATT: 0.988\n```\n:::\n:::\n\n\n## Matching vs weighting\n\nA close cousin of matching is **inverse propensity weighting (IPW)**:\nweight treated units by $1 / \\hat e(X)$ and control units by\n$1 / (1 - \\hat e(X))$, then take the weighted difference in means:\n\n::: {#ipw .cell cache='true' execution_count=9}\n``` {.julia .cell-code}\nw = ifelse.(df.D .== 1, 1 ./ df.ps, 1 ./ (1 .- df.ps))\nw_trim = min.(w, quantile(w, 0.99))\n\natt_ipw = sum(df.Y[df.D .== 1] .* w_trim[df.D .== 1]) / sum(w_trim[df.D .== 1]) -\n          sum(df.Y[df.D .== 0] .* w_trim[df.D .== 0]) / sum(w_trim[df.D .== 0])\n@printf(\"IPW ATT (≈ ATE):       %.3f\\n\", att_ipw)\n@printf(\"Matched ATT (NN):      %.3f\\n\", att)\n@printf(\"Matched ATT (replace): %.3f\\n\", att_repl)\n@printf(\"True ATE:              1.000\\n\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nIPW ATT (≈ ATE):       1.064\nMatched ATT (NN):      1.287\nMatched ATT (replace): 0.988\nTrue ATE:              1.000\n```\n:::\n:::\n\n\nIPW and matching often agree closely. When they disagree, the\ndivergence usually reflects either a misspecified propensity score\n(check with balance diagnostics) or an overlap problem.\n\n## When to use which method\n\n| Method | When to use | Limitation |\n|---|---|---|\n| **NN matching** (no replacement) | Simple, intuitive; ATT estimand | Drops unmatched units |\n| **NN matching** (with replacement) | Few treated, many controls | Wasteful of controls |\n| **IPW** | Good propensity model | Sensitive to extreme weights |\n| **Doubly robust (AIPW)** | Combine matching/IPW with regression | More complex |\n\nFor applied work in Julia, **IPW + AIPW** is usually the more practical\ndefault than 1:1 matching, because Julia's regression and weighting\ninfrastructure (`GLM`, `Panelest`) is mature whereas matching algorithms\nare not. For matched analyses, the R ecosystem (`MatchIt`, `cobalt`) is\nthe workhorse — the [companion R chapter](https://xiangao.github.io/causal_econometrics_guide/matching.html)\nwalks through full matching, CEM, and balance diagnostics in detail.\n\n## Summary\n\n- **Matching** restricts comparisons to similar treated–control pairs,\n  approximating a randomised experiment.\n- **Propensity score nearest-neighbour matching** is the simplest form\n  and implementable in a few lines of Julia.\n- **Balance diagnostics** (standardised mean differences) verify the\n  matching's success.\n- **Estimation**: paired-difference SE on the matched data; regression\n  adjustment on the matched sample (an even better default).\n- For richer matching (optimal, full, CEM) and mature balance tooling,\n  use R's `MatchIt`/`cobalt` ecosystem.\n\n## References\n\n- Stuart, E. A. (2010). Matching methods for causal inference: a review and a look forward. *Statistical Science*, 25(1), 1–21.\n- Iacus, S. M., King, G., & Porro, G. (2012). Causal inference without balance checking: coarsened exact matching. *Political Analysis*, 20(1), 1–24.\n- Ho, D. E., Imai, K., King, G., & Stuart, E. A. (2007). Matching as nonparametric preprocessing for reducing model dependence in parametric causal inference. *Political Analysis*, 15(3), 199–236.\n- Greifer, N. (2024). *MatchIt: Getting Started.* CRAN vignette.\n\n",
    "supporting": [
      "matching_files"
    ],
    "filters": [],
    "includes": {}
  }
}